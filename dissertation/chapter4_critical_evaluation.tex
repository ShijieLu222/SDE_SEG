\chapter{Critical Evaluation}
\label{chap:evaluation}

% ============================================================
\section{Overview}
\label{sec:repro_overview}

\noindent
This chapter is organised in two parts. The first part
(Sections~\ref{sec:repro_protocol}--\ref{sec:stage_b_table7}) provides a
critical evaluation of the reproduction results for Stage~(a) (standard
supervised learning and data selection strategies) and Stage~(b)
(semi-supervised and multi-task learning) based on the framework proposed
by \cite{hoyer2023ijcv_sde}. The second part
(Sections~\ref{sec:ct-calibration-ablation}
and~\ref{sec:ct-component-ablation}) evaluates the proposed Cross-Task
Consistency Loss ($\mathcal{L}_\text{ct}$), which introduces an explicit
feature-alignment objective between the segmentation and depth decoder
branches of the PAD architecture.
Section~\ref{sec:ct-calibration-ablation} calibrates the loss
hyperparameters against the PAD-MTL baseline to identify the most stable
individual configuration and the strongest $d{\rightarrow}s$ design
family. Section~\ref{sec:ct-component-ablation} then evaluates whether
this selected design family composes with the remaining framework
components (data selection and DepthMix).

The primary objectives are: (i)~to verify whether the reported
performance trends of \cite{hoyer2023ijcv_sde} are consistent under a
constrained single-GPU academic computing environment; and
(ii)~to evaluate whether $\mathcal{L}_\text{ct}$ yields reliable,
seed-consistent improvements over the reproduced multi-task baseline
under label-efficient conditions, and whether the benefit persists when
$\mathcal{L}_\text{ct}$ is combined with the existing framework
components (data selection and DepthMix). The main cross-task loss
experiments are conducted on the Cityscapes dataset at the 372-label
(1/8) regime, while the reproduction tables also include the additional
label regimes reported in the original paper. Results are reported as
mean mIoU~(\%) $\pm$ standard deviation across three independent seeds
(7, 25, 42). Reproduction entries are presented as
reproduced~/~{\scriptsize (reported)}, followed by the absolute
discrepancy $\Delta = \text{reproduced} - \text{reported}$. Throughout
this chapter, pp denotes percentage points: for example, an increase from
60.0\,\% to 61.0\,\% mIoU is reported as a $+1.0$\,pp improvement.

% ============================================================
\section{Reproduction Protocol}
\label{sec:repro_protocol}

\noindent
To avoid redundancy, this evaluation follows the design and implementation
described in Chapter~\ref{chap:execution}. Stage~(a) and transfer
experiments use a ResNet-101 encoder with an ASPP-based decoder;
Stage~(b) multi-task and cross-task loss experiments additionally employ
the PAD decoder (Section~\ref{sec:projection-design}). We used the
author-provided fixed subset indices for the 100, 372, and 744 label
regimes. All models were trained for 40,000 iterations using SGD with
polynomial learning-rate decay, and results are reported as mean
mIoU~(\%) $\pm$ standard deviation over three seeds. For Stage~(b)
transfer-related experiments (Table~\ref{tab:table5_repro}), we use the
author-released SDE checkpoints (fd0/fd2) to maintain parity with the
official pipeline.

Baseline values are reported per table under the corresponding experiment
configuration. When baseline values differ across tables, this reflects
differences in the surrounding training pipeline rather than implying
direct numerical comparability across tables.

All experiments in this chapter were conducted under the single-GPU
constraint described in Section~\ref{sec:problem-setup}: batch size~2
without Synchronised Batch Normalisation~(SyncBN). As discussed in
Chapter~\ref{chap:execution}, this reduces the number of samples
available to each batch normalisation step, which is a known source of
instability for semi-supervised methods. This provides the unified
background context for the reproduction gaps observed across subsequent
tables; the following sections quantify these gaps per configuration
without repeating this explanation.

% ============================================================
\section{Stage (a): Data Selection Reproduction (Table 1)}
\label{sec:stage_a_table1}

\noindent
\textbf{Objective.} Stage~(a) assesses the effectiveness of various data
selection strategies under limited label budgets.
Table~\ref{tab:table1_repro} reproduces Table~1 of
\cite{hoyer2023ijcv_sde}, comparing the random baseline against
entropy-based selection and the SDE-guided variants. DS denotes diversity
sampling based on SDE feature distances; US denotes uncertainty sampling
based on the depth-estimation proxy task; DS+US combines both criteria.
These SDE-guided variants follow the data selection design of
\cite{hoyer2023ijcv_sde}.

\begin{table}[H]
\centering
\caption{Reproduction of Table~1 from \cite{hoyer2023ijcv_sde}: data
selection strategies on Cityscapes. mIoU~(\%) is reported as
mean$\pm$std over three seeds. Each cell shows
reproduced~/~{\scriptsize (reported)}, followed by $\Delta$ in
percentage points~(pp).}
\label{tab:table1_repro}
\renewcommand{\arraystretch}{1.6}
\small
\begin{tabularx}{\textwidth}{lXXX}
\toprule
\textbf{Method} &
\makecell[c]{\textbf{1/30} \\ \textbf{(100 labels)}} &
\makecell[c]{\textbf{1/8} \\ \textbf{(372 labels)}} &
\makecell[c]{\textbf{1/4} \\ \textbf{(744 labels)}} \\
\midrule
Random &
\makecell[c]{$47.57 \pm 0.96$ \\ {\scriptsize ($48.75 \pm 1.61$), $\Delta=-1.18$}} &
\makecell[c]{$58.25 \pm 0.43$ \\ {\scriptsize ($59.14 \pm 1.02$), $\Delta=-0.89$}} &
\makecell[c]{$63.22 \pm 0.30$ \\ {\scriptsize ($63.46 \pm 0.38$), $\Delta=-0.24$}} \\
Entropy &
\makecell[c]{$53.76 \pm 0.44$ \\ {\scriptsize ($53.63 \pm 0.77$), $\Delta=+0.13$}} &
\makecell[c]{$63.61 \pm 0.78$ \\ {\scriptsize ($63.51 \pm 0.68$), $\Delta=+0.10$}} &
\makecell[c]{$65.57 \pm 0.62$ \\ {\scriptsize ($66.18 \pm 0.50$), $\Delta=-0.61$}} \\
US &
\makecell[c]{$50.83 \pm 0.91$ \\ {\scriptsize ($51.75 \pm 1.12$), $\Delta=-0.92$}} &
\makecell[c]{$63.07 \pm 0.11$ \\ {\scriptsize ($62.77 \pm 0.46$), $\Delta=+0.30$}} &
\makecell[c]{$66.84 \pm 0.34$ \\ {\scriptsize ($66.76 \pm 0.45$), $\Delta=+0.08$}} \\
DS &
\makecell[c]{$52.67 \pm 0.14$ \\ {\scriptsize ($53.00 \pm 0.51$), $\Delta=-0.33$}} &
\makecell[c]{$62.86 \pm 0.46$ \\ {\scriptsize ($63.23 \pm 0.69$), $\Delta=-0.37$}} &
\makecell[c]{$66.74 \pm 0.15$ \\ {\scriptsize ($66.37 \pm 0.20$), $\Delta=+0.37$}} \\
DS+US &
\makecell[c]{$53.63 \pm 0.19$ \\ {\scriptsize ($54.37 \pm 0.36$), $\Delta=-0.74$}} &
\makecell[c]{$63.38 \pm 0.92$ \\ {\scriptsize ($64.25 \pm 0.18$), $\Delta=-0.87$}} &
\makecell[c]{$66.35 \pm 0.62$ \\ {\scriptsize ($66.94 \pm 0.59$), $\Delta=-0.59$}} \\
\bottomrule
\end{tabularx}
\end{table}

\noindent
\textbf{Observation and Analysis.}
Our reproduction is broadly consistent with the overall trend that informed
selection strategies outperform Random across all label budgets. Relative
to Random, entropy and the depth-guided variants yield substantial gains
under the 100 and 372 label regimes, while the margin narrows at 744
labels, suggesting diminishing returns as supervision increases.
Although the paper reports DS+US as the most consistently effective
strategy, our results indicate that Entropy achieves comparable
performance in the lowest-budget setting. This may suggest that
model-based uncertainty alone provides a reasonably strong selection
signal when annotations are extremely scarce, without necessarily
requiring depth-guided refinement. The remaining absolute gaps to the
paper are consistent with the single-GPU constraint described in
Section~\ref{sec:repro_protocol}, with most configurations falling within
$\pm$1\,mIoU of the paper. Although the exact ranking among the selection
strategies is not fully preserved, the broader trend that informed
selection improves over random sampling is consistently reproduced.

\clearpage
% ============================================================
\section{Stage (b): Data Mixing Strategy Reproduction (Table 3)}
\label{sec:stage_ab_table3}

\noindent
\textbf{Objective.} This section evaluates Stage~(b) mixing strategies
applied to the subsets defined in Stage~(a).
Table~\ref{tab:table3_repro} reproduces Table~3 of
\cite{hoyer2023ijcv_sde}, comparing four training strategies. DX denotes
DepthMix, the geometry-aware mixing strategy introduced by
\cite{hoyer2023ijcv_sde}; ClassMix is a class-segment-based mixing
baseline from \cite{olsson2021classmix}; Pseudo-Labels refers to
mean-teacher self-training without geometric mixing. The experimental
setup follows \cite{hoyer2023ijcv_sde} throughout.

\begin{table}[H]
\centering
\caption{Reproduction of Table~3 from \cite{hoyer2023ijcv_sde}: data
mixing strategies on Cityscapes. mIoU~(\%) is reported as mean$\pm$std
over three seeds. Each entry shows reproduced~/~{\scriptsize (reported)},
followed by $\Delta$ in percentage points~(pp). DX denotes DepthMix, the
geometry-aware mixing strategy introduced by \cite{hoyer2023ijcv_sde}.}
\label{tab:table3_repro}
\renewcommand{\arraystretch}{1.2}
\begin{tabularx}{\textwidth}{lXX}
\toprule
\textbf{Mixing Strategy} &
\textbf{372 Labels (1/8)} &
\textbf{2975 Labels (Full)} \\
\midrule
Baseline &
$58.25 \pm 0.43$ {\scriptsize ($59.14 \pm 1.02$), $\Delta=-0.89$} &
$67.30 \pm 0.53$ {\scriptsize ($67.77 \pm 0.13$), $\Delta=-0.47$} \\
Pseudo-Labels &
$60.05 \pm 0.92$ {\scriptsize ($62.39 \pm 0.86$), $\Delta=-2.34$} &
-- \\
ClassMix &
$63.46 \pm 1.20$ {\scriptsize ($63.16 \pm 0.89$), $\Delta=+0.30$} &
$69.23 \pm 0.51$ {\scriptsize ($69.60 \pm 0.32$), $\Delta=-0.37$} \\
DX (DepthMix) &
$64.57 \pm 1.12$ {\scriptsize ($64.14 \pm 1.34$), $\Delta=+0.43$} &
$70.11 \pm 0.28$ {\scriptsize ($69.83 \pm 0.36$), $\Delta=+0.28$} \\
\bottomrule
\end{tabularx}
\end{table}

\noindent
\textbf{Observation and Analysis.}
The performance hierarchy (DepthMix $>$ ClassMix $>$ Baseline) is
reproduced across both label regimes. The result is consistent with the
interpretation that incorporating geometric scene structure into the
mixing mask may reduce the boundary artefacts typical of ClassMix, where
semantically inconsistent regions can be introduced at the cut boundary.
The largest deviation from the paper is observed for pseudo-labelling
($\Delta = -2.34$), which is considerably larger than all other gaps in
this table. One plausible explanation is that pseudo-label self-training
relies on per-batch confidence statistics to filter low-quality
pseudo-labels at each update step; with a smaller batch, these statistics
are estimated from fewer samples and may therefore be less stable,
allowing more erroneous pseudo-labels to enter training and making
self-training more susceptible to confirmation bias. By contrast, the
mixing masks of ClassMix and DepthMix are derived directly from class
segments or geometric depth estimates, without depending on confidence
statistics, and are thus likely to be more robust to batch size variation.
This observation provides a practical motivation for considering
augmentation-based and multi-task alternatives, which appear more stable
under the same constraint.

% ============================================================
\section{Stage (b): SDE Feature Transfer Reproduction (Table 5)}
\label{sec:stage_ab_table5}

\noindent
\textbf{Objective.} We evaluate the impact of transferring SDE-pretrained
features to the segmentation task, specifically investigating the role of
the feature-distance loss ($\mathcal{L}_F$) and multi-task learning.
The \texttt{fd0} and \texttt{fd2} encoder variants and the
feature-distance regularisation term~$\mathcal{L}_F$ are described in
detail in Section~\ref{sec:fd0-fd2}. The reproduction results for SDE
feature transfer are reported in Table~\ref{tab:table5_repro}. The
Multi-Task ($\mathcal{L}_F=\checkmark$) setting---joint segmentation and
SDE training with the PAD decoder---is used as the
no-$\mathcal{L}_\text{ct}$ baseline for all cross-task loss experiments
in Sections~\ref{sec:ct-calibration-ablation}
and~\ref{sec:ct-component-ablation}.

\begin{table}[H]
\centering
\caption{Reproduction of Table~5 from \cite{hoyer2023ijcv_sde}: SDE
feature transfer methods on Cityscapes. mIoU~(\%) is reported as
mean$\pm$std over three seeds. Each entry shows
reproduced~/~{\scriptsize (reported)}, followed by $\Delta$ in
percentage points~(pp). $\mathcal{L}_F$ denotes the feature-distance
regularisation loss used to mitigate forgetting of SDE-derived
representations during segmentation fine-tuning.}
\label{tab:table5_repro}
\renewcommand{\arraystretch}{1.2}
\begin{tabularx}{\textwidth}{lXX}
\toprule
\textbf{Method} &
\textbf{372 Labels (1/8)} &
\textbf{2975 Labels (Full)} \\
\midrule
Baseline &
$58.25 \pm 0.43$ {\scriptsize ($59.14 \pm 1.02$), $\Delta=-0.89$} &
$67.30 \pm 0.53$ {\scriptsize ($67.77 \pm 0.13$), $\Delta=-0.47$} \\
Transfer (no $\mathcal{L}_F$) &
$59.73 \pm 0.57$ {\scriptsize ($60.46 \pm 0.64$), $\Delta=-0.73$} &
$68.74 \pm 0.67$ {\scriptsize ($69.00 \pm 0.70$), $\Delta=-0.26$} \\
Transfer ($\mathcal{L}_F=\checkmark$) &
$60.83 \pm 0.94$ {\scriptsize ($60.80 \pm 0.69$), $\Delta=+0.03$} &
$68.99 \pm 0.27$ {\scriptsize ($69.47 \pm 0.38$), $\Delta=-0.48$} \\
Multi-Task ($\mathcal{L}_F=\checkmark$) &
$60.75 \pm 0.69$ {\scriptsize ($61.25 \pm 0.55$), $\Delta=-0.50$} &
$69.40 \pm 0.29$ {\scriptsize ($69.76 \pm 0.39$), $\Delta=-0.36$} \\
\bottomrule
\end{tabularx}
\end{table}

\noindent
\textbf{Observation and Analysis.}
Our results are broadly consistent with the core scientific claims of the
original paper: SDE pretraining appears to provide a stronger
initialisation, and $\mathcal{L}_F$ may help mitigate forgetting of the
geometric representations acquired during self-supervised depth
estimation. The near-zero gap for Transfer ($\mathcal{L}_F=\checkmark$)
at 372 labels ($\Delta = +0.03$) is encouraging, suggesting that the
benefit of SDE-derived features transfers robustly across training
environments even under single-GPU constraints.

These results also point to a structural limitation of the MTL setting.
Although the depth and segmentation decoders share the same encoder and
are trained jointly, their intermediate feature representations are never
explicitly required to be mutually consistent. The depth decoder learns
geometric structure that is potentially informative for segmentation
boundaries and object extents, yet this information can only propagate
implicitly through the shared encoder gradients. Consequently, the extent
to which depth-derived geometric knowledge benefits the segmentation
branch depends on the gradient dynamics of the shared encoder, which
remain implicit and are difficult to control directly. This structural
gap provides a clear motivation for the design of
$\mathcal{L}_\text{ct}$, which introduces a learnable projection head to
enforce explicit alignment between the two decoder branches, providing a
more direct and controllable channel for geometric knowledge to
regularise the segmentation task under label-efficient conditions.

% ============================================================
\section{Stage (b): Framework Component Ablation (Table 7)}
\label{sec:stage_b_table7}

\noindent
\textbf{Objective.} We reproduce the full framework component ablation
study to evaluate the synergistic effects of data selection~(S),
DepthMix~(DX), and multi-task learning~(MTL). S denotes the DS+US
automatic data selection strategy; DX denotes DepthMix augmentation; MTL
denotes joint segmentation and SDE training using the PAD decoder for
cross-task feature exchange. Table~\ref{tab:table7_repro} presents a
direct comparison between our results and the values reported in
\cite{hoyer2023ijcv_sde}.

\begin{table}[H]
\centering
\caption{Reproduction of Table~7 from \cite{hoyer2023ijcv_sde}: framework
component ablation on Cityscapes. mIoU~(\%) is reported as mean$\pm$std
over three seeds. Each cell shows reproduced~/~{\scriptsize (reported)},
followed by $\Delta$ in percentage points~(pp). S~=~data selection
(DS+US); DX~=~DepthMix; MTL~=~multi-task learning with the PAD decoder.
Tick marks indicate a component is active; dashes indicate it is absent.}
\label{tab:table7_repro}
\renewcommand{\arraystretch}{2.0}
\small
\begin{tabularx}{\textwidth}{cccXX}
\toprule
\textbf{S} & \textbf{DX} & \textbf{MTL} &
\makecell[c]{\textbf{372 Labels (1/8)}} &
\makecell[c]{\textbf{2975 Labels (Full)}} \\
\midrule
-- & -- & -- &
\makecell[c]{$58.25 \pm 0.43$ \\ {\scriptsize ($59.14 \pm 1.02$), $\Delta=-0.89$}} &
\makecell[c]{$67.30 \pm 0.53$ \\ {\scriptsize ($67.77 \pm 0.13$), $\Delta=-0.47$}} \\
-- & -- & \checkmark &
\makecell[c]{$60.75 \pm 0.69$ \\ {\scriptsize ($61.25 \pm 0.55$), $\Delta=-0.50$}} &
\makecell[c]{$69.40 \pm 0.29$ \\ {\scriptsize ($69.76 \pm 0.39$), $\Delta=-0.36$}} \\
-- & \checkmark & -- &
\makecell[c]{$64.57 \pm 1.12$ \\ {\scriptsize ($64.14 \pm 1.34$), $\Delta=+0.43$}} &
\makecell[c]{$70.11 \pm 0.28$ \\ {\scriptsize ($69.83 \pm 0.36$), $\Delta=+0.28$}} \\
\checkmark & -- & -- &
\makecell[c]{$65.23 \pm 0.46$ \\ {\scriptsize ($64.25 \pm 0.18$), $\Delta=+0.98$}} &
\makecell[c]{--} \\
\checkmark & -- & \checkmark &
\makecell[c]{$66.35 \pm 0.76$ \\ {\scriptsize ($65.35 \pm 0.10$), $\Delta=+1.00$}} &
\makecell[c]{--} \\
\checkmark & \checkmark & -- &
\makecell[c]{$66.99 \pm 0.44$ \\ {\scriptsize ($66.48 \pm 0.27$), $\Delta=+0.51$}} &
\makecell[c]{--} \\
-- & \checkmark & \checkmark &
\makecell[c]{$65.11 \pm 0.51$ \\ {\scriptsize ($66.66 \pm 1.05$), $\Delta=-1.55$}} &
\makecell[c]{$71.28 \pm 0.30$ \\ {\scriptsize ($71.16 \pm 0.16$), $\Delta=+0.12$}} \\
\checkmark & \checkmark & \checkmark &
\makecell[c]{$67.66 \pm 0.47$ \\ {\scriptsize ($68.01 \pm 0.83$), $\Delta=-0.35$}} &
\makecell[c]{--} \\
\bottomrule
\end{tabularx}
\end{table}

\noindent
\textbf{Observation and Synergy Analysis.}
The full ablation indicates that S+DX+MTL achieves the highest performance
at the 372-label regime ($67.66$\,\% mIoU), consistent with the paper's
findings. Our reproduced results broadly recover the qualitative trends
reported in the paper, with most configurations falling within
$\pm$1\,mIoU, and several single-GPU runs marginally exceeding the
reported values---notably S\,+\,MTL (without DX, $\Delta = +1.00$\,pp)
and S\,+\,DX (without MTL, $\Delta = +0.51$\,pp)---which may reflect
favourable random-seed interactions under the fixed subset indices.

A notable discrepancy arises in the DX+MTL configuration at 372 labels,
where our result ($65.11$) falls $1.55$\,pp below the paper ($66.66$),
despite exceeding the DX-only result ($64.57$). One plausible explanation
is that the joint optimisation of DepthMix augmentation and the auxiliary
depth objective is more sensitive to batch size than either component in
isolation. Specifically, DepthMix introduces mixed-geometry boundary
regions whose depth supervision may be noisier than in unmixed samples.
Under a small batch size, the auxiliary depth objective may receive noisier
gradient estimates from these boundary regions, which could interfere with
the segmentation branch via the shared encoder. In the multi-GPU setup of
the original paper, larger effective batch sizes may smooth out this noise,
whereas the single-GPU setup may amplify it.

This pattern reinforces the structural gap discussed after
Table~\ref{tab:table5_repro}: under small-batch conditions, the implicit
path through the shared encoder may be particularly susceptible to
gradient noise introduced by mixed-geometry samples. An explicit
feature-level alignment loss operating directly on decoder
representations may therefore provide a more direct channel for
cross-task knowledge transfer than relying only on gradients propagated
through the shared encoder.

% ============================================================
\section{Cross-Task Consistency Loss: Calibration and Ablation}
\label{sec:ct-calibration-ablation}

\noindent
Building on the reproduced PAD-MTL baseline, this section evaluates the
proposed Cross-Task Consistency Loss ($\mathcal{L}_\text{ct}$), which
aligns intermediate features from the segmentation and depth decoder
branches via a learnable $1{\times}1$ convolutional projection head.
We report results from two experimental phases: a single-seed calibration
sweep (Phase~I) over loss type, projection layout, projection direction,
and $\lambda_\text{ct}$; and a three-seed validation (Phase~II) for the
most promising configurations. All $\Delta$ values are reported as
absolute mIoU differences in percentage points~(pp).

\noindent
\textbf{Notation for this section.}
Throughout Sections~\ref{sec:ct-calibration-ablation}
and~\ref{sec:ct-component-ablation}, the following terms are used
consistently. \textit{Single projection} denotes a layout with one
$1{\times}1$ convolutional projection head. The direction of projection is
written as $d{\rightarrow}s$ when depth decoder features
($f_\text{depth}$) are projected into the segmentation feature space, and
$s{\rightarrow}d$ for the reverse direction. \textit{Dual projection} uses
two independent $1{\times}1$ projection heads applied simultaneously in
both directions, so both consistency losses are summed; this doubles the
additional parameter count from $16{,}512$ to $33{,}024$. Unless otherwise
stated, the baseline for the cross-task loss experiments is the reproduced
PAD-MTL configuration (Multi-Task, $\mathcal{L}_F=\checkmark$, no
$\mathcal{L}_\text{ct}$) from Table~\ref{tab:table5_repro}. For the
component-level ablation in Section~\ref{sec:ct-component-ablation}, each
$\mathcal{L}_\text{ct}$ variant is compared against its matching
no-$\mathcal{L}_\text{ct}$ baseline under the same framework components.

% ============================================================
\subsection{Phase I: Single-seed Calibration (Seed~7)}
\label{subsec:ct-phase1-seed7}

\noindent
The Phase~I sweep for $\mathcal{L}_\text{ct}$ covers three factors:
loss type (cosine similarity vs.\ normalised MSE), projection layout
(single vs.\ dual $1{\times}1$ convolutional heads), and
$\lambda_\text{ct}\in\{0.25, 0.50, 0.75, 1.00\}$. For the single
projection layout, two projection directions are additionally evaluated:
single~$(d{\rightarrow}s)$, in which the depth feature is projected into
the segmentation space, and single~$(s{\rightarrow}d)$, in which the
segmentation feature is projected into the depth space.

For the single-projection setting, this gives two directions, two loss
types, and four values of $\lambda_\text{ct}$, resulting in
$2 \times 2 \times 4 = 16$ configurations. For the dual-projection
setting, there is no separate direction choice, resulting in
$2 \times 4 = 8$ configurations. Therefore, Phase~I evaluates
24 distinct configurations in total.

Phase~I fixes random seed~7 to screen all configurations; the goal is
not to identify a globally optimal configuration, but to eliminate
underperforming regions of the search space and reduce the candidate
set to a tractable size for multi-seed validation in Phase~II.
Seed~7 was selected because it corresponds to the median-performing seed
in the PAD-MTL baseline ($59.84$\,\%, compared to $60.92$\,\% and
$61.50$\,\% for seeds~25 and~42), providing a conservative reference.
All Phase~I $\Delta$ values use $\text{baseline}_{\text{S7}} = 59.84$\,\%.
The single-projection calibration results are reported in
Table~\ref{tab:ct-p1-single}, while the dual-projection results are
reported in Table~\ref{tab:ct-p1-dual}.

\begin{table}[H]
\centering
\caption{Phase~I single-seed calibration (\textit{seed}~$=7$): single
projection layout, both directions and both loss types, across
$\lambda_\text{ct} \in \{0.25, 0.50, 0.75, 1.00\}$. Baseline is
PAD-MTL without $\mathcal{L}_\text{ct}$ at seed~7 (59.84\,\%). $\Delta$
values are in percentage points~(pp) relative to this baseline. See the
notation paragraph above for definitions of $d{\rightarrow}s$ and
$s{\rightarrow}d$.}
\label{tab:ct-p1-single}
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{llccc}
\toprule
Direction & Loss & $\lambda_\text{ct}$ & mIoU (\%) & $\Delta$\,(pp) \\
\midrule
Baseline (seed~7) & -- & -- & 59.84 & -- \\
\midrule
$d{\rightarrow}s$ & cosine & 0.25 & 62.89 & $+3.05$ \\
$d{\rightarrow}s$ & cosine & 0.50 & 61.42 & $+1.58$ \\
$d{\rightarrow}s$ & cosine & 0.75 & 63.00 & $+3.16$ \\
$d{\rightarrow}s$ & cosine & 1.00 & 63.77 & $+3.93$ \\
\midrule
$d{\rightarrow}s$ & MSE & 0.25 & 61.97 & $+2.13$ \\
$d{\rightarrow}s$ & MSE & 0.50 & 61.68 & $+1.84$ \\
$d{\rightarrow}s$ & MSE & 0.75 & 61.46 & $+1.62$ \\
$d{\rightarrow}s$ & MSE & 1.00 & 62.16 & $+2.32$ \\
\midrule
$s{\rightarrow}d$ & cosine & 0.25 & 62.70 & $+2.86$ \\
$s{\rightarrow}d$ & cosine & 0.50 & 62.55 & $+2.71$ \\
$s{\rightarrow}d$ & cosine & 0.75 & 61.26 & $+1.42$ \\
$s{\rightarrow}d$ & cosine & 1.00 & 61.30 & $+1.46$ \\
\midrule
$s{\rightarrow}d$ & MSE & 0.25 & 62.08 & $+2.24$ \\
$s{\rightarrow}d$ & MSE & 0.50 & 62.64 & $+2.80$ \\
$s{\rightarrow}d$ & MSE & 0.75 & 61.83 & $+1.99$ \\
$s{\rightarrow}d$ & MSE & 1.00 & 63.08 & $+3.24$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[H]
\centering
\caption{Phase~I single-seed calibration (\textit{seed}~$=7$): dual
projection layout, both loss types, across
$\lambda_\text{ct} \in \{0.25, 0.50, 0.75, 1.00\}$. Dual projection
uses two independent $1{\times}1$ heads simultaneously in both
directions ($33{,}024$ additional parameters, versus $16{,}512$ for
single projection). Baseline mIoU $= 59.84$\,\%. $\Delta$ values are in
percentage points~(pp).}
\label{tab:ct-p1-dual}
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{lccc}
\toprule
Loss & $\lambda_\text{ct}$ & mIoU (\%) & $\Delta$\,(pp) \\
\midrule
Baseline (seed~7) & -- & 59.84 & -- \\
\midrule
cosine & 0.25 & 61.76 & $+1.92$ \\
cosine & 0.50 & 63.66 & $+3.82$ \\
cosine & 0.75 & 62.45 & $+2.61$ \\
cosine & 1.00 & 63.32 & $+3.48$ \\
\midrule
MSE & 0.25 & 62.77 & $+2.93$ \\
MSE & 0.50 & 62.56 & $+2.72$ \\
MSE & 0.75 & 63.07 & $+3.23$ \\
MSE & 1.00 & 62.48 & $+2.64$ \\
\bottomrule
\end{tabular}
\end{table}

\noindent
\textbf{Phase-I findings.}
All 24 configurations improve over the seed-7 baseline, suggesting that
$\mathcal{L}_\text{ct}$ provides a positive signal in this single-seed
screen. However, these results are treated only as calibration evidence,
and multi-seed validation is required before drawing conclusions about
stability.

For single projection, the $d{\rightarrow}s$ direction achieves a higher
Phase~I peak than $s{\rightarrow}d$: $63.77$\,\% vs.\ $63.08$\,\%
respectively. For $d{\rightarrow}s$, cosine loss with
$\lambda_\text{ct}=1.00$ reaches the per-direction maximum. In contrast,
for $s{\rightarrow}d$, the cosine peak occurs at $\lambda_\text{ct}=0.25$
($62.70$\,\%), and higher $\lambda_\text{ct}$ values reduce performance
(down to $61.26$--$61.30$\,\% at $\lambda=0.75/1.00$). This indicates
that the $s{\rightarrow}d$ direction is more sensitive to
$\lambda_\text{ct}$ in the single-seed screen.

For dual projection, the peak is $63.66$\,\% (cosine, $\lambda=0.50$),
which is competitive with but below the single $d{\rightarrow}s$ peak.
The dual MSE results are more stable across $\lambda_\text{ct}$ values
than dual cosine. Although the dual-projection peaks are notable,
Phase~I is a single-seed screen; whether these peaks generalise is
evaluated in Phase~II.

Based on these results, Phase~II carries forward selected high-performing
regions from Phase~I: single $d{\rightarrow}s$ cosine and MSE around the
best-performing loss scales; single $s{\rightarrow}d$ cosine and MSE
around their strongest Phase~I settings; and dual cosine at
$\lambda \in \{0.50, 0.75, 1.00\}$. This enables a direct comparison of
projection direction, projection layout, and loss formulation under
multi-seed conditions.

% ============================================================
\subsection{Phase II: Three-seed Validation}
\label{subsec:ct-phase2-3seeds}

\noindent
Phase~I and Phase~II should be interpreted as two separate experimental
stages. Phase~I is a single-seed screening stage used to identify promising
regions of the search space, whereas Phase~II reruns the selected
candidates under the finalised validation protocol across three seeds.
Therefore, Phase~I values are used for candidate selection only and are not
treated as directly paired with the corresponding seed-7 values in
Phase~II.

Using the candidates identified in Phase~I, we train with three
independent seeds (7, 25, 42) to obtain more reliable seed-level estimates
of mean performance and variance. Per-seed best validation mIoU, mean,
standard deviation, and $\Delta$ versus the 3-seed baseline mean are
reported in Table~\ref{tab:ct-phase2-3seeds}, sorted by mean mIoU in
descending order.

\begin{table}[H]
\centering
\caption{Phase~II three-seed validation (372 labels), sorted by mean
mIoU (descending). Baseline is PAD-MTL without $\mathcal{L}_\text{ct}$,
retrained with the same three seeds (7, 25, 42) in the same submission
batch. $\Delta$~(pp) $= \text{mean} - 60.75$, where $60.75$\,\% is the
three-seed baseline mean.}
\label{tab:ct-phase2-3seeds}
\renewcommand{\arraystretch}{1.3}
\small
\begin{tabularx}{\textwidth}{Xcccccr}
\toprule
Configuration &
Seed~7 & Seed~25 & Seed~42 & Mean & Std & $\Delta$\,(pp) \\
\midrule
Baseline (PAD-MTL) &
59.84 & 60.92 & 61.50 & 60.75 & 0.69 & -- \\
\midrule
single $(d{\rightarrow}s)$, MSE, $\lambda=1.00$ &
62.86 & 62.91 & 64.91 & 63.56 & 0.95 & $+2.81$ \\
single $(d{\rightarrow}s)$, cosine, $\lambda=1.00$ &
63.01 & 63.37 & 63.37 & 63.25 & 0.17 & $+2.50$ \\
single $(s{\rightarrow}d)$, cosine, $\lambda=0.75$ &
62.82 & 62.54 & 64.00 & 63.12 & 0.63 & $+2.37$ \\
single $(d{\rightarrow}s)$, cosine, $\lambda=1.25$ &
62.44 & 62.24 & 64.33 & 63.00 & 0.94 & $+2.25$ \\
dual cosine, $\lambda=0.75$ &
63.22 & 62.91 & 62.73 & 62.95 & 0.20 & $+2.20$ \\
single $(s{\rightarrow}d)$, cosine, $\lambda=0.50$ &
62.46 & 62.39 & 63.89 & 62.91 & 0.69 & $+2.16$ \\
single $(s{\rightarrow}d)$, MSE, $\lambda=1.00$ &
61.82 & 62.52 & 64.29 & 62.88 & 1.04 & $+2.13$ \\
dual cosine, $\lambda=0.50$ &
62.54 & 62.91 & 63.15 & 62.87 & 0.25 & $+2.12$ \\
single $(s{\rightarrow}d)$, MSE, $\lambda=0.50$ &
61.97 & 62.46 & 64.08 & 62.84 & 0.90 & $+2.09$ \\
single $(d{\rightarrow}s)$, cosine, $\lambda=0.75$ &
63.08 & 62.89 & 62.48 & 62.82 & 0.25 & $+2.07$ \\
dual cosine, $\lambda=1.00$ &
62.77 & 61.81 & 63.63 & 62.74 & 0.74 & $+1.99$ \\
single $(s{\rightarrow}d)$, MSE, $\lambda=0.75$ &
61.08 & 62.78 & 63.96 & 62.61 & 1.19 & $+1.86$ \\
single $(s{\rightarrow}d)$, cosine, $\lambda=1.00$ &
61.81 & 62.01 & 63.07 & 62.30 & 0.55 & $+1.55$ \\
\bottomrule
\end{tabularx}
\end{table}

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{figures/phase2_validation.pdf}
  \caption[Phase~II three-seed validation results]{Phase~II three-seed
  validation at the 372-label regime. Bars show mean mIoU with standard
  deviation; the red line denotes the PAD-MTL baseline ($60.75$\,\%), and
  the highlighted bar marks the selected single $d{\rightarrow}s$ cosine
  configuration ($\lambda_\text{ct}=1.00$).}
  \label{fig:phase2-validation}
\end{figure}

\noindent
\textbf{Phase-II findings.}
\begin{itemize}

\item \textit{$\mathcal{L}_\text{ct}$ is robustly beneficial across the
  tested configurations.}
  As shown in Figure~\ref{fig:phase2-validation}, all thirteen validated
  configurations improve over the baseline mean ($60.75$\,\%), with gains
  ranging from $+1.55$ to $+2.81$\,pp.
  This suggests that $\mathcal{L}_\text{ct}$ provides a consistent
  positive signal across the tested architectural choices and
  hyperparameter settings, although the limited number of seeds means
  the result should be interpreted as robustness evidence rather than a
  formal statistical guarantee.

\item \textit{The highest raw mean does not correspond to the most stable
  configuration.}
  Single~$(d{\rightarrow}s)$ MSE at $\lambda=1.00$ achieves the highest
  mean of $63.56$\,\%, outperforming the selected cosine variant by
  $0.31$\,pp. However, its standard deviation is substantially larger
  ($0.95$\,\% vs.\ $0.17$\,\%), and the high mean is strongly influenced
  by seed~42 ($64.91$\,\%). By contrast, single~$(d{\rightarrow}s)$
  cosine at $\lambda=1.00$ achieves a slightly lower mean
  ($63.25$\,\%) but with tightly clustered seed values
  ($63.01$ / $63.37$ / $63.37$). For this reason, the cosine variant is
  treated as the more reliable configuration.

\item \textit{Projection direction affects stability and loss-scale
  sensitivity.}
  The best $s{\rightarrow}d$ configuration in Phase~II is cosine with
  $\lambda=0.75$, achieving $63.12$\,\% with std $0.63$\,\%.
  This is competitive with but slightly below the selected
  $d{\rightarrow}s$ cosine configuration. In contrast,
  $s{\rightarrow}d$ cosine at $\lambda=1.00$ drops to $62.30$\,\%,
  suggesting that the reverse direction is more sensitive to the loss
  scale. This supports the interpretation that projecting depth features
  into the segmentation space provides a more stable optimisation target
  in the PAD-MTL baseline setting.

\item \textit{MSE loss produces competitive means but higher variance
  than cosine.}
  MSE variants achieve strong raw performance, particularly
  single~$(d{\rightarrow}s)$ MSE at $\lambda=1.00$ ($63.56$\,\%) and
  single~$(s{\rightarrow}d)$ MSE at $\lambda=1.00$ ($62.88$\,\%).
  However, their standard deviations are considerably higher than the
  best cosine variants. This suggests that MSE is more sensitive to
  random initialisation and training dynamics, whereas cosine similarity
  provides a more stable angular alignment objective across seeds.

\item \textit{Single $(d{\rightarrow}s)$ cosine, $\lambda=1.00$ is
  selected as the primary configuration.}
  This configuration achieves mean $63.25$\,\% ($\Delta=+2.50$\,pp)
  with the lowest standard deviation of any tested variant ($0.17$\,\%),
  with seed values tightly clustered at $63.01$ / $63.37$ / $63.37$.
  This stability indicates the improvement is consistent and
  reproducible rather than seed-dependent, and it is therefore
  selected as the primary design choice for downstream experiments.
  Notably, while single~$(d{\rightarrow}s)$ MSE achieves the highest
  raw mean ($63.56$\,\%), its higher variance makes it less suitable as
  the stability-anchored primary choice; both MSE and cosine within the
  $d{\rightarrow}s$, $\lambda_\text{ct}=1.00$ design family are
  carried forward into the composability analysis of
  Section~\ref{sec:ct-component-ablation}, as the interaction between
  $\mathcal{L}_\text{ct}$ and stronger framework components may shift
  the relative behaviour of the two loss formulations.

\item \textit{Dual-projection results fall below the best stable
  single-projection result.}
  The best dual variant (dual cosine, $\lambda=0.75$) achieves mean
  $62.95$\,\% ($+2.20$\,pp), approximately $0.30$\,pp below the
  selected single $(d{\rightarrow}s)$ configuration, while doubling
  the additional parameters from $16{,}512$ to $33{,}024$.
  The asymmetric structure of single projection---where one branch
  remains in its original feature space while the other is projected---may
  provide a more constrained alignment target than dual projection, where
  both feature spaces are allowed to move simultaneously.

\item \textit{Phase~I single-seed peaks do not always generalise.}
  The Phase~I peak for dual cosine ($\lambda=0.50$: $63.66$\,\%)
  drops to a three-seed mean of $62.87$\,\%, a gap of $0.79$\,pp.
  Similarly, the best Phase~I $s{\rightarrow}d$ setting
  (MSE, $\lambda=1.00$: $63.08$\,\%) reaches a lower three-seed mean
  of $62.88$\,\%. This underscores the necessity of multi-seed
  validation before drawing conclusions on optimal $\lambda_\text{ct}$,
  and supports the two-phase design of the evaluation protocol.

\end{itemize}

% ============================================================
\subsection{Summary and Conclusions}
\label{subsec:ct-summary}

\noindent
The two-phase evaluation supports the following conclusions:

\begin{enumerate}

\item \textit{Effectiveness.} Across all validated configurations,
  $\mathcal{L}_\text{ct}$ improves over the PAD-MTL baseline,
  indicating that explicit cross-task feature alignment is beneficial
  for label-efficient segmentation within the present setup. The gains
  are robust across the tested projection layouts, projection directions,
  loss types, and $\lambda_\text{ct}$ values.

\item \textit{Single projection with depth-to-seg alignment is the most
  reliable design on the PAD-MTL baseline.}
  The highest raw mean is obtained by single~$(d{\rightarrow}s)$ MSE
  with $\lambda_\text{ct}=1.00$ ($63.56$\,\%), but its standard
  deviation is substantially larger than that of the cosine variant
  ($0.95$\,\% vs.\ $0.17$\,\%). The strongest stable configuration is
  therefore single~$(d{\rightarrow}s)$ cosine with
  $\lambda_\text{ct}=1.00$, which achieves mean $63.25$\,\% with the
  lowest observed variance. The best $s{\rightarrow}d$ variant, cosine
  with $\lambda_\text{ct}=0.75$, remains competitive ($63.12$\,\%) but
  does not exceed the selected $d{\rightarrow}s$ cosine configuration in
  either mean performance or stability.

\item \textit{Cosine loss shows more reliable behaviour than MSE.}
  MSE variants achieve competitive or even higher raw means, but their
  standard deviations are consistently higher than the most stable cosine
  configuration. Cosine similarity, being scale-invariant, provides a
  more stable alignment objective across random seeds, whereas MSE is
  more sensitive to seed-dependent feature magnitude and optimisation
  dynamics.

\item \textit{Stable $\lambda_\text{ct}$ range.}
  For single $(d{\rightarrow}s)$ cosine, $\lambda_\text{ct}=1.00$
  offers the best balance of performance and stability across three
  seeds. The adjacent value of $0.75$ still improves over the baseline
  with low variance, indicating that the method is not highly sensitive
  to the exact choice of $\lambda_\text{ct}$ within this range. However,
  as shown in Table~\ref{tab:ct-phase2-3seeds}, the $s{\rightarrow}d$
  direction shows stronger sensitivity to the loss scale, with cosine
  performance dropping notably at $\lambda_\text{ct}=1.00$ (from
  $63.12$\,\% at $\lambda=0.75$ to $62.30$\,\% at $\lambda=1.00$).

\end{enumerate}

% ============================================================
\section{Cross-Task Consistency Loss: Composability with Framework Components}
\label{sec:ct-component-ablation}

\noindent
The calibration study in Section~\ref{sec:ct-calibration-ablation}
identified single~$(d{\rightarrow}s)$ projection with
$\lambda_\text{ct}=1.00$ as the most stable design track on the PAD-MTL
baseline, with cosine similarity selected as the primary loss formulation
due to its lower variance. This section evaluates whether the benefit of
$\mathcal{L}_\text{ct}$ persists when it is combined with the remaining
framework components, namely data selection~(S) and DepthMix~(DX). The
main composability analysis fixes the projection direction and
$\lambda_\text{ct}$ to the calibrated values ($d{\rightarrow}s$,
$\lambda_\text{ct}=1.00$) and evaluates both MSE and cosine loss
formulations. Both are retained because, within the $d{\rightarrow}s$,
$\lambda_\text{ct}=1.00$ family, MSE achieved the highest raw mean while
cosine achieved the most stable result in Phase~II, and their relative
ordering may change under stronger framework components.

\vspace{4pt}
\noindent
\textbf{Objective.}
We ask whether $\mathcal{L}_\text{ct}$ continues to improve segmentation
performance once stronger baselines are introduced, or whether its
benefit is subsumed by other components. This is a composability
question: even if $\mathcal{L}_\text{ct}$ improves over pure PAD-MTL,
its contribution could be redundant with the geometric priors introduced
by DepthMix or with the higher-quality supervision introduced by data
selection. The ablation therefore covers three component combinations on
top of PAD-MTL: MTL\,+\,DX, MTL\,+\,S, and MTL\,+\,DX\,+\,S. For each
combination, the main reported ablation compares the matching baseline
against both MSE and cosine variants within the fixed
$d{\rightarrow}s$, $\lambda_\text{ct}=1.00$ design family. All
$\mathcal{L}_\text{ct}$ variants use single projection and
bidirectional gradient flow.

\vspace{4pt}
\noindent
\textbf{Design of the mean-level comparison.}
The baseline rows in each group of
Table~\ref{tab:ct-component-ablation} are trained under the same
pipeline and in the same submission batch as the corresponding
$\mathcal{L}_\text{ct}$ variants. Each $\Delta$ is reported as the
difference between the three-seed mean of the $\mathcal{L}_\text{ct}$
variant and the three-seed mean of the matching
$\mathcal{L}_\text{ct}$-free baseline. This controls for the framework
configuration, although the comparison should be interpreted as a
mean-level component ablation rather than a formal statistical test.

\begin{table}[H]
\centering
\caption[Component-level ablation of $\mathcal{L}_\text{ct}$]{Component-level ablation of $\mathcal{L}_\text{ct}$ on three
PAD-MTL component combinations at 372 labels. S~=~data selection
(DS+US); DX~=~DepthMix; MTL~=~multi-task learning with the PAD decoder.
All $\mathcal{L}_\text{ct}$ variants use single $d{\rightarrow}s$
projection with $\lambda_\text{ct}=1.00$; both MSE and cosine loss
formulations are evaluated because they were respectively the
highest-mean and most stable candidates within this design family in
Phase~II. Each baseline row (Direction~$=$~--,
$\mathcal{L}_\text{ct}$~$=$~none) is trained in the same submission batch
as its paired variants. Results are three-seed mean
mIoU~(\%) $\pm$ standard deviation; $\Delta$ values are in percentage
points~(pp) relative to the matching baseline.}
\label{tab:ct-component-ablation}
\renewcommand{\arraystretch}{1.3}
\small
\begin{tabular}{cccccccc}
\toprule
\textbf{S} & \textbf{DX} & \textbf{MTL} &
\textbf{Direction} & $\boldsymbol{\mathcal{L}_\text{ct}}$ &
\textbf{Mean} & \textbf{Std} & $\boldsymbol{\Delta}$\textbf{(pp)} \\
\midrule
-- & \checkmark & \checkmark & -- & none &
65.11 & 0.51 & -- \\
-- & \checkmark & \checkmark & $d{\rightarrow}s$ & MSE &
65.77 & 0.51 & $+0.66$ \\
-- & \checkmark & \checkmark & $d{\rightarrow}s$ & Cosine &
66.08 & 1.19 & $+0.97$ \\
\midrule
\checkmark & -- & \checkmark & -- & none &
66.35 & 0.76 & -- \\
\checkmark & -- & \checkmark & $d{\rightarrow}s$ & MSE &
66.95 & 0.18 & $+0.60$ \\
\checkmark & -- & \checkmark & $d{\rightarrow}s$ & Cosine &
66.45 & 0.75 & $+0.10$ \\
\midrule
\checkmark & \checkmark & \checkmark & -- & none &
67.66 & 0.47 & -- \\
\checkmark & \checkmark & \checkmark & $d{\rightarrow}s$ & MSE &
68.19 & 1.03 & $+0.53$ \\
\checkmark & \checkmark & \checkmark & $d{\rightarrow}s$ & Cosine &
67.93 & 0.59 & $+0.27$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{figures/component_ablation.pdf}
  \caption[Component-level ablation of $\mathcal{L}_\text{ct}$]{Component-level ablation of $\mathcal{L}_\text{ct}$ at the
  372-label regime. Bars compare matching baselines with single
  $d{\rightarrow}s$ MSE and cosine variants; values above bars indicate
  mean mIoU gains.}
  \label{fig:component-ablation}
\end{figure}

\vspace{4pt}
\noindent
\textbf{Findings.}
\begin{itemize}

\item \textit{$\mathcal{L}_\text{ct}$ shows positive mean-level gains
  across all three component combinations in the selected
  $d{\rightarrow}s$ track.}
  As shown in Figure~\ref{fig:component-ablation}, all six reported
  $\mathcal{L}_\text{ct}$ variants improve over their matching baselines
  in terms of three-seed mean mIoU, with gains ranging from $+0.10$ to
  $+0.97$\,pp. However, the smallest gain is within the observed seed
  variation and should be interpreted cautiously. Overall, the mean-level
  trend suggests that the proposed loss can provide additional gains under
  stronger baselines, rather than being fully subsumed by data selection
  or DepthMix.

\item \textit{The relative ordering of MSE and cosine is
  component-dependent.}
  On MTL\,+\,S, MSE achieves higher gains than cosine for the
  $d{\rightarrow}s$ direction ($+0.60$ vs.\ $+0.10$\,pp). When
  DepthMix is introduced, cosine becomes stronger on MTL\,+\,DX
  ($+0.97$\,pp vs.\ $+0.66$\,pp), while on MTL\,+\,DX\,+\,S the MSE
  variant reaches the higher mean within $d{\rightarrow}s$
  ($68.19$\,\% vs.\ $67.93$\,\%). This confirms that evaluating both
  loss formulations within the selected design family is necessary, as
  neither dominates consistently across all component combinations.

\item \textit{The largest benefit arises on MTL\,+\,DX, which is the
  configuration with the largest reproduction gap.}
  The reproduced MTL\,+\,DX baseline lies $1.55$\,pp below the paper
  (Table~\ref{tab:table7_repro}), the most pronounced discrepancy in
  the Table~7 reproduction. Adding $\mathcal{L}_\text{ct}$ recovers a
  substantial fraction of this gap: the best variant
  ($d{\rightarrow}s$ cosine) closes it by $+0.97$\,pp. This is
  consistent with the interpretation that DepthMix may produce
  mixed-geometry boundary regions whose depth supervision is noisier
  under small batch sizes, and an explicit alignment loss may provide a
  more stable channel for geometric knowledge transfer when the
  implicit path through the shared encoder is disrupted.

\item \textit{Within the selected design family, the strongest
  configuration is $d{\rightarrow}s$ MSE on MTL\,+\,DX\,+\,S.}
  This configuration attains $68.19$\,\%, exceeding the reproduced
  full-framework baseline ($67.66$\,\%) by $+0.53$\,pp. Compared with the
  paper's reported S\,+\,DX\,+\,MTL value ($68.01$\,\%), the difference is
  $+0.18$\,pp, which is small relative to observed seed variation.
  Therefore, the appropriate interpretation is that the single-GPU result
  is comparable to the multi-GPU reference, rather than a definitive
  outperformance claim.

\end{itemize}

\vspace{4pt}
\noindent
\textbf{Summary.}
This ablation suggests that the benefit of $\mathcal{L}_\text{ct}$
observed in Section~\ref{sec:ct-calibration-ablation} is not specific to
the pure PAD-MTL setting. As visualised in
Figure~\ref{fig:component-ablation}, all three component combinations
show positive mean-level gains from the proposed loss within the selected
$d{\rightarrow}s$, $\lambda_\text{ct}=1.00$ track; the gain is largest
where the reproduction is weakest (MTL\,+\,DX); and the best
full-framework configuration within this track becomes comparable to the
original paper's reported value under the constrained single-GPU setting.

% ============================================================
\section{Chapter Summary}
\label{sec:chapter4_summary}

\noindent
This chapter has evaluated the project's experimental work from two
perspectives: the quality of the reproduction of the original framework,
and the effectiveness of the proposed $\mathcal{L}_\text{ct}$ extension.

With respect to reproduction, the main qualitative performance trends from
the four reproduced result tables corresponding to Tables~1, 3, 5, and~7
of \cite{hoyer2023ijcv_sde} are largely preserved under the single-GPU
constraint. The reproduced results broadly recover the findings of
\cite{hoyer2023ijcv_sde}, with most configurations falling within
$\pm$1\,mIoU of the reported values and several single-GPU runs
marginally exceeding them. Where under-reproduction occurs, the
reduced batch size and absence of SyncBN provide plausible contributing
factors, since both are known to affect the stability of semi-supervised
training.

With respect to the extension, $\mathcal{L}_\text{ct}$ produces positive
mean improvements across all validated configurations. The highest raw
Phase~II mean is obtained by single~$(d{\rightarrow}s)$ MSE,
$\lambda_\text{ct}=1.00$, but its higher variance makes it less reliable
for model selection in isolation. The calibrated primary
design---single $(d{\rightarrow}s)$ cosine, $\lambda_\text{ct}=1.00$---achieves a
consistent, low-variance gain of $+2.50$\,pp over the PAD-MTL baseline
across three independent seeds, as visualised in
Figure~\ref{fig:phase2-validation}. Both cosine and MSE within the
selected $d{\rightarrow}s$, $\lambda_\text{ct}=1.00$ design family were
carried forward into the composability analysis. The results confirm
positive mean-level gains across all three framework component
combinations and show that the best full-framework result
($68.19$\,\% mIoU, $d{\rightarrow}s$ MSE on MTL\,+\,DX\,+\,S) is
comparable to the original paper's multi-GPU reference under the
constrained single-GPU setting used in this project.